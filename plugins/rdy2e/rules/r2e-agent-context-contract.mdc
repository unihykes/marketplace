---
description: Agent 上下文契约：路径占位符与默认解析约定
alwaysApply: true
---

## 指令(做什么)

在编写或执行涉及 r2e 路径的 SKILL、agents、rules、终端命令时，使用本文件定义的
`<项目路径>`、`<插件路径>`、`<技能名称>`、`<技能路径>`、`<相对路径>` 及其「目录」同义写法（若有），保持全仓口径一致。

占位符为对模型的说明，**不是** shell 环境变量；除非另行通过安装脚本或终端 `export` 配置，
否则**不会**自动注入系统环境。

其余占位符与契约条款后续在本文件「约束」一节补充。

## 约束(怎么做)

### `<项目路径>` / `<项目目录>`

- **含义**：当前指令所针对的**用户工程根目录**（Cursor 工作区中该仓库的根路径）。
- **典型用法**：命令在「项目根」下执行、相对路径相对此根解析。
- **多根工作区**：若存在多个工作区根，以**本对话当前操作所在**的那一个为准；无法在上下文中确定时，应写绝对路径或由用户确认。

### `<插件路径>` / `<插件目录>`

- **含义**：本机已安装的 **r2e Cursor 插件根目录**
  （展开后应同时存在 `.cursor-plugin/`、`rules/`、`skills/`、`agents/`、`hooks/` 等）。
- **默认值**（与仓库 `install.sh` 默认目标一致）：
  - Linux：`$HOME/.cursor/plugins/local/r2e`
  - Windows：`%USERPROFILE%\.cursor\plugins\local\r2e`
- **自定义安装**：若安装时使用 `install.sh <PROJECT_ROOT>`（或 Windows 等价安装），则
  `<插件路径>` = `<PROJECT_ROOT>/.cursor/plugins/local/r2e`（分隔符随平台）。

### `<技能名称>`

- **含义**：原子技能在 `skills/` 下的**目录名标识**（无斜杠的单一片段），与磁盘目录 `skills/<技能名称>/`、
  该技能 `SKILL.md` frontmatter 中 `name` 字段一致。
- **与 `<技能路径>`**：`<技能路径>` = `<插件路径>/skills/<技能名称>`；单独写 `<技能名称>` 时仅指名称片段，**不**表示完整路径。
- **示例**：`r2e-code-build`、`r2e-code-check`。

### `<技能路径>` / `<技能目录>`

- **含义**：某一原子技能在插件内的**根目录**，即 `<插件路径>/skills/<技能名称>`。
- **引用范围**：`<技能路径>` / `<技能目录>` **仅在当前技能的 `SKILL.md` 正文中**使用，表示**该**
  **`SKILL.md` 所属技能**在插件内的根目录（与`SKILL.md`文件所在目录一致）。
- **路径差异**：不同技能的 `SKILL.md` 位于 `skills/` 下不同子目录，故各文件中占位符解析得到的
  **绝对路径不同**；不得脱离具体 `SKILL.md` 假定全技能共用同一物理路径。
- **示例**：技能 `r2e-code-build` 的 `<技能路径>` 为 `<插件路径>/skills/r2e-code-build`。
- **说明**：「`<技能目录名>`」与「`<技能名称>`」同指该技能在 `skills/` 下的目录名，与 `SKILL.md` 的 `name` 一致。

### `<相对路径>`

- **含义**：在技能或命令中，表示相对**项目根**（见 `<项目路径>` / `<项目目录>`）的目录或路径片段。
- **合法性**：若用户或调用方指定 `<相对路径>`，则其**必须**为相对项目根的路径片段；**不得**为以 `/` 开头的绝对路径
  （以及 Windows 下以盘符 `X:\` 等形式开头的绝对路径）。
