---
description: 单行长度上限 120：代码按语法折行，文档按语句拆分
alwaysApply: true
---

## 指令(做什么)

在本工作区内**新建或修改文本文件**时，控制**单行字符数不超过 120**（按常见 UTF-8 显示宽度计，不含行尾换行符）。

按文件性质分两类的处理方式如下。

## 约束(怎么做)

### 代码文件

**适用范围**：以程序语法为主的源文件或等价文本，常见扩展名包括但不限于：
`*.c`、`*.cpp`、`*.h`、`*.hpp`、`*.cc`、`*.cxx`、`*.py`、`*.go`、`*.rs`、`*.java`、
`*.ts`、`*.tsx`、`*.js`、`*.jsx`、`*.json`、`*.yaml`、`*.yml`、`*.sh` 等。

- 任一物理行不得超过 **120 个字符**。
- 超长时须在**语义合理**处断行，例如：函数参数列表在逗号后换行、模板实参分层、链式调用在 `.` 或 `->` 前换行、长表达式在二元运算符后换行等。
- **禁止**为凑长度在标识符、字符串字面量、数字字面量或关键 token 中间硬断；必要时先调整结构（临时变量、子表达式抽取）再换行。

### 文档文件

**适用范围**：以自然语言叙述为主的文件（例如 `*.md`、`*.mdx`、`*.txt`、`README*`、无固定扩展名的说明类纯文本等）。

- 任一物理行不得超过 **120 个字符**。
- 超长时须在**语句边界或停顿处**拆成多行，优先顺序为：句号、问号、叹号、分号、冒号、逗号、顿号后的空白处；英文可在从属连词或短语边界处断开并保持可读。
- **禁止**为凑长度在单词正中间随意割裂英文单词；中文避免在不成词的单个字处断开，除非原排版即为列举项换行。

### 通用

- 若某行因缩进导致「内容区」变短，仍以**整行**不超过 120 字符为准。
- 若文件类型难以归类，先按**文档文件**规则处理（保守按标点与短语边界折行），避免破坏语法结构。
