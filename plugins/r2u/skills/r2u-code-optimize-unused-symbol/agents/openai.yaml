interface:
  display_name: "r2u-code-optimize-unused-symbol"
  short_description: "检查未被使用的代码符号并给出清理建议；供 code-optimize 类 agent 主动调用"
  default_prompt: "Use $r2u-code-optimize-unused-symbol to 检查未被使用的代码符号并给出清理建议；供 code-optimize 类 agent 主动调用"

policy:
  allow_implicit_invocation: false

