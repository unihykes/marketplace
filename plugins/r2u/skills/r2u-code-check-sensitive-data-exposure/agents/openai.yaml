interface:
  display_name: "r2u-code-check-sensitive-data-exposure"
  short_description: "检查敏感数据暴露风险，供code-check类型的agent主动调用"
  default_prompt: "Use $r2u-code-check-sensitive-data-exposure to 检查敏感数据暴露风险，供code-check类型的agent主动调用"

policy:
  allow_implicit_invocation: false

