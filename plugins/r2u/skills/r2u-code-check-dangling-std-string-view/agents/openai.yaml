interface:
  display_name: "r2u-code-check-dangling-std-string-view"
  short_description: "检查 std::string_view 绑定临时或即将失效缓冲的悬垂风险；供 code-check 类 agent 主动调用"
  default_prompt: "Use $r2u-code-check-dangling-std-string-view to 检查 std::string_view 绑定临时或即将失效缓冲的悬垂风险；供 code-check 类 agent 主动调用"

policy:
  allow_implicit_invocation: false

